<?xml version="1.0" standalone="no"?>
<kickstart>

<description>
  Disk partitioning information for a cluster compute node.
</description>

<main>
</main>

<pre>
echo "clearpart --all --initlabel --drives=sda
part /    --label root --size 16384 --ondisk sda
part /var --label var  --size 4096  --ondisk sda
part swap --size 4096  --ondisk sda
part /state/partition1 --label scratch1 --size 1 --grow --ondisk sda
" &gt;  /tmp/user_partition_info
</pre>

</kickstart>

